# Residuals

## Activation rejected

The one repaired permission route closed as
`camera_authorization_activation_rejected` with furthest stage
`activation_requested`. Authorization status and `requestAccess` were not
reached. Functional result is `fail` with residual class `environment or
signing residual`; no authorization decision exists.

## Computer Use limitation

Computer Use could not attach to the exact active Debug bundle or the system
permission surface within bounded attempts. The generic HoldType surface was
the protected pre-existing installed app and was not operated. No prompt or
Allow control was safely addressable, so no UI action occurred and no fallback
or retry was used.

## Cleanup deviation

One exact run-owned Debug process outlived the script terminal. After fresh
PID/executable validation, TERM was sent only to that process and it exited
within the bounded cleanup wait. No process residue remains; independent review
must decide whether the post-terminal lifetime is a Debug-spike cleanup defect.
