# R07 residuals

| Residual | Class | Disposition |
| --- | --- | --- |
| The sole hardware route emitted `invalid_configuration` before an attempt event | debug-spike defect | Authorization, audio, camera, probe, finalization, and preservation were not reached. The closed category does not expose the failed configuration check, so no subsystem attribution is made. |
| W07 publication returned `publisher_validation_mismatch` | debug-spike defect | No validated snapshot existed and the consumer was not invoked. Post-terminal inspection established zero files/media and only the expected empty private directory shells. |
| No realized media or preservation dimension exists | product-threshold input awaiting Phase 0C | All media and quantitative fields remain blank and `evidence_only`; no values are reconstructed. |
| No visible/audible controlled markers were present | product-threshold input awaiting Phase 0C | Sync offset and drift are unavailable and no claim is made. |

The exact empty implicated roots were removed after classification using only
their validated run-owned tokens and non-recursive directory removal. One
unrelated pre-existing Phase 0B temporary root was preserved. No permission
request, TCC action, fallback, transcode, downsample, second microphone owner,
capture retry, media file, or Ready clip occurred.

Next dependency: `DV-P0B-CAPTURE-R07-REVIEW`.
