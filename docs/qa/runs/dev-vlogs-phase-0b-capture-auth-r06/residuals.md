# Residuals

## Activation timeout

The single accepted permission route closed as
`camera_authorization_activation_timed_out` with furthest stage
`activation_requested`. Authorization status and `requestAccess` were not
reached. Functional result is `fail` with residual class `environment or
signing residual`; no permission or capture authority exists.

## Computer Use observation boundary

The route closed before an exact run-owned Debug surface could be safely
attached. The post-terminal read-only app-list check exposed only the protected
pre-existing installed HoldType surface, which was not operated. Because
`requestAccess` was not reached, no genuine prompt action was available. No
retry, generic-app attachment, or fallback interaction was used.

## W05 supervision result

The one direct marker-owned process had `script-sibling` topology, exited
naturally, and was reaped. No additional identity, fallback signal, uncertainty,
root retention, or process residue occurred. The R05 additional-process cleanup
defect did not reproduce in this invocation.
