# Residuals

## Authorization timeout

The one accepted request-mode invocation closed as
`camera_authorization_timed_out`. The authorization callback did not return
within the accepted operational deadline, so the final Camera authorization
state is unknown. This is a functional failure with residual class
`environment or signing residual`; it is not a grant, denial, or restriction.

## Prompt and Computer Use limitation

Computer Use first exposed only the pre-existing installed HoldType surface,
which was preserved and not operated. Its one bounded attach to the exact
signed Debug authorization identity timed out. Prompt visibility was therefore
unavailable and no Allow, denial, dismissal, System Settings, or other UI
action occurred. No alternate automation surface or second request was used.

## Unexercised domains

Camera enumeration, camera capture, microphone/audio ownership, media,
finalization, probes, storage, and product behavior were not run. This packet
makes no capture or authorization-state claim beyond the closed timeout.
