# Residuals

## Activation timeout

The single accepted permission route closed as
`camera_authorization_activation_timed_out` with furthest stage
`activation_requested`. Authorization status and `requestAccess` were not
reached. Functional result is `fail` with residual class `environment or
signing residual`; no permission or capture authority exists.

## Computer Use limitation

Computer Use could not attach to the exact active Debug bundle within its
bounded attempt. No genuine prompt or Allow control was safely addressable, so
no UI action occurred and no fallback or retry was used.

## Additional process cleanup

The accepted direct app PID exited naturally and supervisor TERM/KILL fallback
was not used. A separate run-owned Debug process from the same launch remained
after script exit. After fresh exact identity validation, cleanup TERM was sent
only to that process and it exited within the bounded wait. No residue remains;
independent review must classify this additional-process lifetime.
