# R06 residuals

| Residual | Class | Disposition |
| --- | --- | --- |
| `stored_sample_exact_v1` returned `video_preservation_failed` after both media probes and passthrough finalization passed | debug-spike defect | The route collapses every typed comparator/reader/timeout mismatch to one generic category, and its failed terminal omits prior probe metrics and `videoEvidence`; the exact failed dimension and realized metrics are permanently unavailable from R06 even if the watcher had succeeded. |
| The redacted ad-hoc watcher retained no run-owned raw event stream before accepted cleanup | debug-spike defect | Raw event timing/path evidence is separately unavailable; the unretained watcher leaves wrong nested-path glob versus cleanup race unobservable. This did not cause the terminal route to omit probe metrics or preservation detail. |
| No visible/audible controlled markers were present | product-threshold input awaiting Phase 0C | Sync offset and drift are unavailable and no claim is made. |
| Start latency, byte rate, resource use, and finalization overhead were not recovered | product-threshold input awaiting Phase 0C | Failed terminal evidence omits these fields, and the watcher separately retained no raw event timing; evidence-only fields remain blank. |
| Three bounded wrapper preflights ended before actual enumeration | environment or signing residual | Exact roots/processes were removed; they did not enumerate, capture, or consume the one hardware attempt. |

The signed Debug application, product owners, and external storage were not
modified. No permission request, System Settings action, TCC reset, direct TCC
database operation, fallback, transcode, downsample, second microphone owner,
capture retry, or Ready clip occurred.

Next dependency: `DV-P0B-CAPTURE-R06-REVIEW-R1`.
