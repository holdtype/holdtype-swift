# R08 residuals

| Residual | Class | Disposition |
| --- | --- | --- |
| The sole hardware route emitted `invalid_configuration` at `event_log_path_mismatch` before an attempt event | debug-spike defect | W08 identifies the failed configuration dimension. Authorization, audio, camera, probe, finalization, and preservation were not reached. The evidence does not identify why the resolved event-log path mismatched, so no deeper attribution is made. |
| No realized media or preservation dimension exists | product-threshold input awaiting Phase 0C | All media and quantitative fields remain blank and `evidence_only`; no values are reconstructed. |
| No visible/audible controlled markers were present | product-threshold input awaiting Phase 0C | Sync offset and drift are unavailable and no claim is made. |

The W08 configuration diagnostic was published once, validated, consumed once,
and removed through the accepted W07-R3/W08 authority. The raw script root and
private enumeration root were removed after classification; the unrelated
pre-existing Phase 0B root was preserved. No permission request, TCC action,
fallback, transcode, downsample, second microphone owner, capture retry, media
file, or Ready clip occurred.

Next dependency: `DV-P0B-CAPTURE-R08-REVIEW`.
