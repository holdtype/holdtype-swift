# Historical App-Private Recovery Lifecycle

- Node type: leaf
- Status: Historical
- Read when: tracing legacy launch-recovery ordering and owner composition.
- Do not read when: implementing current recovery without current contracts.
- Maximum size: 100 physical lines.

One composition scheduler/storage context, credential coordinator, failed-History
service, Settings/Library owners served all scenes; views created no fallback or
repository and root failure showed blocking storage unavailable, never defaults.
Credential unavailability did not block provider-free surfaces.

Launch scheduled one coalesced provider-free bounded opportunity plus marked temp
cleanup. It ordered failed-Retry cold barrier, exact process-lost output destination
reconciliation with directory-durable absence, retained cutover cleanup, History
cleanup, then conversion of provider phases to explicit recovery. Partial identity,
corruption, unavailable protection remained Pending. It never read Keychain,
requested mic, recorded, built provider, contacted OpenAI, published keyboard, or
inserted. Pending work retried only on later lifecycle, never loops/background promise.

Current V1.1 one-Pending/Latest and current launch recovery take precedence.
