# Dev Vlogs Acceptance And Residuals

- Node type: leaf
- Status: Active
- Parent contract: `holdtype.macos.dev-vlogs@DV-ACTIVE-6`
- Clauses: `DV-ACC-*`, `DV-FINAL-QA`
- Read when: claiming implementation readiness, acceptance, release, or selecting the next capability.
- Do not read when: only contract meaning is needed without a capability claim.
- Maximum size: 100 physical lines.

- Setup/UI remains Debug development and excluded from `1.0.11`; product tests
  and Computer Use remain future-shipping evidence. Incomplete `DV-FINAL-QA`
  cannot block a release excluding the feature.
- Capture is ready under ACTIVE-3 but acceptance needs product bundle-ID policy,
  camera lifecycle, one shipping audio lease, exact-once, configured passthrough,
  playable `1V/0A` + `1V/1A`, truthful format, dictation independence, and failures.
- Storage mechanics may advance; protected-scope acceptance and numeric warning/
  stop wait for safe measurement. Prior controlled R05 changed protected metadata
  with unknown cause and is not an accepted protected-scope pass.
- Daily Publish needs newest-first days, summaries, exact Finder mapping,
  observation/Refresh, no Library/editor, and result-action QA.
- Build/share needs action-time reconstruction/order, recipe-before-render,
  cancel/retry, missing/corrupt/incompatible sources, immutable playable result,
  unchanged source, Reveal, and Share.
- Phases: 1 Debug setup; 2 one-clip capture; 3 Finder daily review; 4 local
  build/export/Share. No Phase-0B expansion without explicit user approval.
