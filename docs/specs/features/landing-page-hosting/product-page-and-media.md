# Landing Product Page And Media

- Node type: leaf
- Status: Active
- Parent contract: `holdtype.website.hosting@2`
- Clauses: `WEBSITE.COPY`, `WEBSITE.MEDIA`, `WEBSITE.ACCESSIBILITY`
- Read when: hero, public claims, setup/download, social preview, tutorial, or screenshot modal is in scope.
- Do not read when: only deployment/DNS is in scope.
- Maximum size: 100 physical lines.

- Hero has one free macOS download action; source is footer. Navigation shows
  adjacent Patreon then GitHub icons, localized names/mobile text; Patreon URL
  is `https://www.patreon.com/c/playphraseme`.
- Copy leads with dictation into the current Mac app, followed by concrete
  writing, translation, dictionary, and selected-text Fixes uses. It does not
  position HoldType as a competitor clone or claim undefined full control.
- Supporting cards explain direct OpenAI billing, the released default model,
  optional correction (off by default), Mac→OpenAI delivery, and local storage.
  Exact `gpt-transcribe` appears once, verbatim in the second-card heading,
  never metadata/other copy. No unmeasured speed/accuracy/competitor claim;
  `BLAZING-FAST` in the existing social artwork is nonbenchmark.
- Fictional `$1M ARR` editor scene is explicitly illustration, not Codex result;
  toolbar is one-line/truncated and does not duplicate body title.
- Stable GitHub latest DMG and full project-tap Homebrew copy remain. The hero
  badge prices the app at `$0` and discloses separately paid OpenAI requests.
  The cost example explicitly assumes 100 dictations total 17 audio minutes:
  ≈`$0.08`, or ≈`$2.30` when repeated daily for 30 days, at `$0.0045/minute`.
  It excludes optional correction, translation, and Fixes, and is not a cap
  or typical-duration claim. Billing estimates locally priced requests across
  categories, not the provider invoice, balance, or complete account usage.
- Last Result recovery is conditional on its save setting. Ordinary recording
  retention defaults off; local recovery audio may survive relaunch. Optional
  cache retention is separate. App privacy claims do not describe website analytics.
- Written official key setup is sufficient and never asks website for key.
  YouTube loads only after Play via privacy-enhanced domain; no-JS link works.
- Shared 1200×630 English PNG has absolute canonical URL and localized alt.
- Screenshot opens original in shared in-page modal; Close/Escape/outside,
  scroll lock, background removed from focus, focus restoration with pointer
  outline suppressed but keyboard focus visible, viewport fit, link cursor.

## Copy delta

`WEBSITE-COPY-2026-09-08` / Evolve: user approved the Russian copy review and
all-locale publication. Revision 2 replaces clone/negative framing with concrete
uses, retains the founder voice and fictional SaaS illustration, and reconciles
model, usage, and recovery disclosures against release `v1.0.11`. This changes
website copy only; app behavior, locale routes, assets, actions, and update feed
remain protected. Validate all locale catalogs/generated metadata and inspect
representative desktop/mobile translations before confirming production.
