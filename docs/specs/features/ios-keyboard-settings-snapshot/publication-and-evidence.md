# Deferred Keyboard Settings Publication And Evidence

- Node type: leaf
- Status: Historical; Deferred
- Read when: reviewing retired fallback, publication, or M0B requirements.
- Do not read when: claiming current static preference publication support.
- Maximum size: 100 physical lines.

Extension reload at appearance/context changes validated schema and values.
Missing/corrupt/unsupported/unavailable state fell back to ordinary Unicode and
required keyboard switching without app, network, secrets, or Full Access; it
never crashed, blocked typing, or presented Phase-0 en-US as approved layout.
Errors logged compact schema/revision categories only.

Publication failure preserved canonical app settings and prior snapshot, showed
truthful refresh state, and retried the current revision without rollback. Risk-
reducing disable immediately revoked new authorization; risk-increasing enable
required successful matching publication and still needed per-result/live gates.
One canonical writer serialized scenes and strictly increased revision.

Diagnostics could expose scalar revision health only. Settings could say
published, never consumed/applied. Voice readiness remained in expiring session
state. Unsupported old snapshot/layout used fallback without partial application
or content logging.

M0B required physical iPhone/iPad evidence with Full Access off/on, real
provisioning, reinstall/upgrade, eviction, missing/corrupt/incompatible App
Group. Failure kept bundled fallback and did not request Full Access merely for
static preferences. Evidence also covered atomic round trip, revision/order,
optional defaults, action-readiness without configuration disclosure, forbidden
encoded fields, and no app/network dependency. No extension writer shipped.
