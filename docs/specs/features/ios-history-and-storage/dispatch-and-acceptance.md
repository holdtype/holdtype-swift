# Historical Dispatch And App-Only Acceptance

- Node type: leaf
- Status: Historical
- Read when: tracing retired Pending provider authority or P4 completion.
- Do not read when: implementing current Voice replay or output repositories.
- Maximum size: 100 physical lines.

Explicit eligible start/Retry atomically committed a fresh transcription UUID
and current compact settings while preserving attempt/audio/creation/duration/
intent. Only that commit minted a process-local one-shot authorization for one
registered executor task. A bounded descriptor-backed reader exposed format,
size, duration, and pread chunks, never path/URL/handle or reusable provider
capability.

Cancellation was registered before launch and won late-result races. Any
terminal result invalidated the reader/lease. Process loss destroyed authority;
load or a persisted transcribing phase could not recreate it. The same process-
owned transaction facade owned subsequent phase and acceptance transitions;
scenes/adapters could not substitute stores.

P4 app-only acceptance committed a generation-0/history-null delivery, verified
the destination, removed exact Pending audio and journal with separate durable
absence, then published resultReady. Any uncertain checkpoint remained Saving
Result with provider-free Retry Saving Result. A durable destination could not
fall back to provider Retry; no destination allowed explicit recovery only
after reservations/intent/uncertainty were proven absent.

Launch could retire an exact ordinary outputDelivery Pending owner only against
its matching accepted destination and metadata. It never republished accepted
text, repeated provider work, read credentials, or treated unlink/missing path
alone as completed ownership.
