# Voice Draft Recovery And Verification

- Node type: leaf
- Status: Active
- Contract: `holdtype.ios.voice-draft.recovery@1`
- Read when: Draft recovery, accessibility/appearance, or acceptance evidence matters.
- Do not read when: only normal Draft mutation semantics matter.
- Maximum size: 100 physical lines.

Loaded Draft stays visible/copyable during dictation outage. A stable blocker
opens its exact Settings owner once; unresolved return creates no navigation
loop and keeps direct recovery. OpenAI/transcription/translation/privacy routes
scroll to exact field with contextual explanation. Keyboard/Full Access uses
dedicated setup, public Settings path, Open System Settings, practice field,
and truthful “not currently verified.” Missing Full Access blocks only keyboard
voice/Fixes, not foreground Draft/Fixes/Copy/safe Latest.

Capture/Pending exposes only controller-admitted Recover/Retry/confirmed Discard.
Draft failure preserves last confirmed view and offers safe Retry. History-save/
cleanup warnings remain nonblocking after acceptance.

VoiceOver names editable area, actions, and disabled reasons; concise nonvisual
outcomes reserve no layout. Dynamic Type may stack actions without clipping.
Reading policy applies editable/read-only; Reduce Motion makes type/follow-tail
changes immediate. Light/Dark geometry matches; Increase Contrast strengthens
native boundaries; Reduce Transparency removes glow. Activity PNG retains
transparent outer pixels, unfiltered full-color cyan/purple, no theme bitmap
background/disabled opacity; native labels/SF Symbols stay crisp/localized.

Tests prove bounded decoding, exact-once append, atomic Clear/restore, collision,
no durable Undo; state load/mutations/meaningful Undo/Redo/branch invalidation/
CAS/failure; all presentation/replacement/append/adaptive/follow-tail states and
no post-action notice. Fixed-size geometry keeps Draft/viewport/Voice bounds and
activity center identical across actions/outcomes. Simulator covers focus/edit/
Done/dismissal, Copy/Clear/Undo, overflow/follow-tail/selection, phases/recovery,
appearances/type/motion/transparency. Signed iPhone proves microphone and Full
Access; phase motion never claims metering.
